import { createSlice } from "@reduxjs/toolkit";

const tweetsSlice = createSlice({
  name: "Tweets",
  initialState: [            {
    id : string| number,
    text: string,
    createdat: date,
    name : string,
    username: string,
    url : string,
}
]],
  reducers: {},
});
