import React from "react";
import { useSelector } from 'react-redux';

const LoginContainer = (props) => {
    {loading, login, data} = useSelector(state=> state.user)
  return;
};

export default LoginContainer;
